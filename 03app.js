// HTTP MODULE

const http = require("http");
/* const http = require("http");

const server = http.createServer((req, res) => {
  if (req.url === "/") {
    res.end("Hello world");
  }
  if (req.url === "/about") {
    res.end("this is about node");
  }
  res.end(`
  <h1>Oops!</h1>
  <a href="/">back</a>
  `);
});

server.listen(5000); */
